# Workspace Rules

## Browser Subagent Directives
- **Do NOT use `browser_get_dom` or DOM extraction tools**.
- Rely strictly on direct URL navigation, pixel clicks, visual verification, and screenshots.
